import './Button.css';
import Button from 'react-bootstrap/Button';

function Button() {
    return (
        <div >
            <Button
                size="lg"
                className='button'
                onClick={''}
            >{''}</Button>
        </div>
    );
}

export default Button;
